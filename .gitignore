node_modules/
dist/
coverage/
.vite/
playwright-report/
test-results/
e2e/.auth/
*.log
.DS_Store
# Raw Codex CLI generations (PNG) are committed with the manifest (SPEC §15.6); agent logs are not.
assets/.gen/**/*.last.md
assets/.gen/**/*.log
tools/.scratch
# Local secrets (OPENROUTER_API_KEY=... for tools/gen-assets.ts --backend openrouter)
.env.local
